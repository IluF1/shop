model Card {
    id          Int    @id @default(autoincrement())
    name        String
    description String
    price       Int
    mainImage   String
    images      String
    cartId      Int
    cart        Cart   @relation(fields: [cartId], references: [id])
}
